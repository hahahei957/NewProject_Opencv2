
"""
    油价预测问题
        获取数据
            通过函数下载数据，如果文件存在目录中，直接返回数据的路径
            通过pandas读取路径中的文件数据
            返回读取结果
        数据预处理
            数据处理
                删去存在空值的数据
                将目标值那一列从表中拆出来
            特征值处理
                将发源地那一列的特征数值化，这里是变成one-hot编码形式
                正则化、归一化处理，防止某些特征值太大造成权重太小的情况出现，所以全都类似的正则话到-1~1之前
                将特征按照比例拆分出测试架和训练集
        构件预测模型
            搭建网络层
                通过Dense直接在Sequential中创建每一层的网络、在其中使用激活函数 relu，得到模型
                返回附带网络层的模型
                注：由于这里是一个回归问题而非分类问题，目标值是连续的，所以最后的全连接层输出的output只有一个值
            构建训练模型
                使用网络层中得到的附带网络层模型
                准备优化器(指定学习率)、损失函数(被优化的对象)
                通过compile函数，将优化器和损失函数加入到模型中，这样模型就有了可迭代的方式，之中优化器和损失函数可以使用程序自带的
                通过损失函数不断反向优化权重，来得到一个很好的预测模型
        训练模型(此时模型内部已经搭建完成，只要进行权重参数的训练即可)
            通过fit函数，不断给上面已经建立好的模型投喂数据进行训练
            顺便打印一下每次训练的日志，看一下训练的好坏
        得到训练好的模型，进行测试评估
            通过model.predict看看预测的好坏
"""